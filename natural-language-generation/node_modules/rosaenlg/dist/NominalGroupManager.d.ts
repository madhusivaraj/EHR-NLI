import { VerbsManager } from './VerbsManager';
import { ValueManager, ValueParams } from './ValueManager';
import { AdjectiveManager } from './AdjectiveManager';
import { Languages } from './NlgLib';
interface SubjectVerbParams extends ValueParams {
    invertSubjectVerb: boolean;
}
export declare class NominalGroupManager {
    private language;
    private verbsManager;
    private valueManager;
    private adjectiveManager;
    private spy;
    constructor(language: Languages, verbsManager: VerbsManager, valueManager: ValueManager, adjectiveManager: AdjectiveManager);
    setSpy(spy: Spy): void;
    subjectVerb(subject: any, verbInfo: any, params: SubjectVerbParams): void;
    subjectVerbAdj(subject: any, verbInfo: any, adjective: string, params: any): void;
}
export {};
