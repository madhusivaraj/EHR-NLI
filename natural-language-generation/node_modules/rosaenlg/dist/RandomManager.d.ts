export declare class RandomManager {
    private incrRandomer;
    private rndNextPos;
    private rndTable;
    private rndEngine;
    constructor(randomSeed: number);
    getRndNextPos(): number;
    setRndNextPos(rndNextPos: number): void;
    incrRnd(incr: number): void;
    getNextRnd(): number;
    private getItemWeight;
    private getSumOfWeights;
    private getTargetIndex;
    private getWeightedRandom;
    randomNotIn(max: number, weights: any, excludes: number[]): number;
}
