export declare type Genders = 'M' | 'F';
export declare type Numbers = 'S' | 'P';
export interface WordInfo {
    G: Genders;
    S: string;
    P: string;
}
export interface WordsInfo {
    [key: string]: WordInfo;
}
export declare function getWordInfo(wordsList: WordsInfo, word: string): WordInfo;
export declare function getNumberItalianWord(wordsList: WordsInfo, word: string, number: Numbers): string;
export declare function getGenderItalianWord(wordsList: WordsInfo, word: string): Genders;
