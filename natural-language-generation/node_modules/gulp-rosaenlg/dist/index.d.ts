/// <reference types="node" />
import fs = require('fs');
import { PackagedTemplate, CompileInfo, Autotest, Languages } from 'rosaenlg-server-toolkit';
export interface PackagedTemplateParams {
    templateId: string;
    entryTemplate: string;
    compileInfo: CompileInfo;
    autotest?: Autotest;
}
export declare function renderTemplateInFile(template: string, dest: string, options: any): void;
interface SourceAndName {
    source: string;
    name: string;
}
export declare function compileTemplates(sourcesAndNames: SourceAndName[], language: Languages, dest: string, holderName: string, tinyify: boolean): fs.WriteStream;
export declare function packageTemplateJson(params: PackagedTemplateParams): PackagedTemplate;
export {};
